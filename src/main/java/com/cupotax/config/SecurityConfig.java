package com.cupotax.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Configuración simplificada - sin Spring Security para evitar errores
    // La autenticación se maneja desde Firebase directamente
}